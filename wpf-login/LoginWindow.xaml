<Window x:Class="WpfApp1.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="LoginWindow" Height="600" Width="600"
        >
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="서버 IP 주소"/>
            <TextBox Name="TxtIp" Text="127.0.0.1" Margin="5"/>
            <TextBlock Text="포트 번호"/>
            <TextBox Name="TxtPort" Text="1234" Margin="5"/>
            <TextBlock Text="닉네임"/>
            <TextBox Name="TxtId" Text="Player1" Margin="5"/>
            <Button Content="게임 접속" Click="BtnConnect_Click" Margin="10"/>
			<Button Content="방 파기" Click="BtnCreateRoom_Click" Margin="10" Background="LightBlue"/>
        </StackPanel>
    </Grid>
</Window>
